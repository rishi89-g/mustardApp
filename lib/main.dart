import 'package:flutter/material.dart';

import 'mysplachscreen.dart';

void main() => runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      home: const MySplashScreen(),
      theme: ThemeData(primarySwatch: Colors.yellow),
    ));
